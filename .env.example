# === Shared ===
SUPABASE_URL=
SUPABASE_ANON_KEY=

# === Backend-only (keep private; do NOT expose in Vite) ===
SUPABASE_SERVICE_ROLE=
JWT_SECRET=change_me
EMBED_PROVIDER=gemini
EMBED_MODEL=text-embedding-004
EMBED_DIM=768
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-latest
GEMINI_VISION_MODEL=gemini-2.5-flash-latest

# === Frontend/Vite (prefix with VITE_) ===
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Backend base (full URL once backend is live)
VITE_API_BASE=
