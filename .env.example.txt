# =============================================================================
# Core API Configuration
# =============================================================================
API_URL=http://localhost:8000

# =============================================================================
# Google Gemini AI Configuration
# =============================================================================
# Primary API key for Gemini (Google AI)
GOOGLE_API_KEY="your-google-api-key-here"

# Alternative API key name (for compatibility)
# GEMINI_API_KEY="your-gemini-api-key-here"

# Gemini model to use for Q&A (recommended: gemini-1.5-flash for speed/accuracy balance)
GEMINI_MODEL=gemini-1.5-flash

# =============================================================================
# Supabase Configuration
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
SUPABASE_SERVICE_ROLE=your-supabase-service-role-key
SUPABASE_DOCS_BUCKET=user-docs

# =============================================================================
# Embedding Configuration
# =============================================================================
# Embedding provider: "gemini" or "local" (for sentence-transformers)
EMBED_PROVIDER=gemini

# Embedding model (for Gemini)
EMBED_MODEL=text-embedding-004

# Embedding dimensions (must match the model)
EMBED_DIM=768

# Local embedding model (if using EMBED_PROVIDER=local)
# SBERT_MODEL=sentence-transformers/all-MiniLM-L6-v2

# =============================================================================
# Q&A Configuration
# =============================================================================
# Temperature for Q&A responses (0.0 = deterministic, 1.0 = creative)
QA_TEMPERATURE=0.5

# Maximum tokens in Q&A responses
QA_MAX_TOKENS=768

# Maximum context characters to send to Gemini
QA_MAX_CONTEXT=7000

# Maximum characters per snippet
QA_PER_SNIPPET=900

# =============================================================================
# Development Configuration
# =============================================================================
# Allow anonymous access (for development only)
# ALLOW_ANON=false

# Gemini context character budget
# GEMINI_CONTEXT_CHAR_BUDGET=12000

# Gemini maximum output tokens
# GEMINI_MAX_TOKENS=1024
